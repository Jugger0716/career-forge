# HANDOFF — 슬라이스 B: 프롬프트 계층 콜드 리뷰 8건 닫힘 + 문서 위치 재편 + 원격 첫 푸시

**Date:** 2026-08-20  **Project:** career-forge (devcareer-prep 플러그인)
**Branch:** harness/devcareer-prep-plugin-2  **HEAD:** 95c4a619dd3a435c8142fcccaad41fa968599507 — chore(docs): redact a real address from the cold review before the branch goes public
**Dirty:** clean  **Upstream:** `origin/harness/devcareer-prep-plugin-2`와 **동기(0/0)**. 원격 `main`은 `31e603d` 그대로이며 이 브랜치가 **35커밋 앞섬** — 병합하지 않았다.

## Goal

개발자의 Git 히스토리를 분석해 경력 기술서 → 지식맵 → 갭 리포트 → 학습·코테 계획을 만드는
Claude Code 플러그인(`devcareer-prep`)을 구축한다. 핵심 가치는 **할루시네이션 방지** — 모든 사실적
주장이 실존하는 커밋 해시나 신뢰 가능한 출처로 뒷받침되어야 하고, 그 검증을 LLM 판단이 아니라
**결정적 스크립트**가 수행한다.

에픽은 `foundation-first` 3슬라이스다. ①은 완료됐고 ②가 진행 중이다. 직전 세션이 프롬프트 계층을
세우고 게이트 3건을 닫았으며, **이 세션은 그 프롬프트 계층에 처음으로 콜드 리뷰를 돌려 8건을 닫고,
문서 위치 규약을 재편하고, 브랜치를 원격에 처음 올렸다.**

## Current State (verified)

- **커밋 5개** — `e743735`(집행 결함) → `3bf0cd5`(프롬프트 계층 + 오라클) → `4cb236b`(서술 정정·문서)
  → `1e9f31d`(문서 위치 재편) → `95c4a61`(PII 가림). `git log --oneline e4e5a51..HEAD`로 확인.
  워킹 트리 **clean**.
- **원격에 처음 올렸다.** `git push -u origin harness/devcareer-prep-plugin-2` → 새 브랜치 생성,
  업스트림 설정 완료(0/0). **`main`은 건드리지 않았다** — 병합·PR은 사용자 지시가 없었다.
- **4게이트 녹색** — `npm run lint` exit 0 / 스모크 **445 PASS** / `--negative` **33 PASS** /
  `--golden` **11 PASS**, 전부 0 FAIL. 세션 시작은 433/33/11이었다.
- **같은 4게이트를 새 클론에서도 확인했다.** `git clone --no-hardlinks` 후 그 안에서 lint 0 /
  445 / 33 / 11. `docs/harness/`가 클론에 없는 것도 확인했다. 이번 회차의 최대 위험이
  「로컬은 녹색, 클론은 빨강」이었으므로 통과 사실을 로컬에서만 보고 넘기지 않았다.
- **커밋 3개가 각자 그 시점에 녹색임을 워크트리로 확인했다** — `e743735` 438 PASS,
  `3bf0cd5` 442, `4cb236b` 442, 전부 0 FAIL + lint 0. 중간 커밋이 빨간색이면 bisect가 무의미하다.
- **프롬프트 계층 콜드 리뷰(라운드 1, 대상 `cc99ea9..e4e5a51`)** — thorough 모드 워크플로
  (전문 리뷰어 3명 병렬 → 적대적 교차검증 3건 → 종합), frontier 구성. 결과 **0 critical /
  2 major / 3 minor / 1 suggestion**, 판정 `COMMENT`, 라운드 판정 `CONDITIONAL PASS`.
  **교차검증에서 반증 탈락 0건, 심각도 조정 0건.** 리포트는
  `docs/harness/cc99ea9-e4e5a51/review_report.md`이며 **이제 추적되지 않는다**(아래 재편 참조).
- **범위 표기를 바로잡고 시작했다.** 직전 핸드오프가 「이번 4커밋(`0b44f24..e4e5a51`)」이라고
  적었지만 그 git 표기는 `0b44f24`를 **제외**해 3커밋만 잡는다. 실제 4커밋을 덮는
  `cc99ea9..e4e5a51`로 돌렸다.
- **게이트 E-4가 거짓 전제 위에서 닫혀 있었다 — 이번 회차의 가장 중요한 발견.** SKILL.md 7단계의
  `verify-evidence.mjs` 호출에 `--identity`도 `--config`도 없어서, 문자 그대로 실행하면
  `[오류] selectedIdentities가 비어 있습니다` + exit 2로 **인용 검증 축에 도달하기 전에** 죽는다.
  즉 「`--stage` 자칭을 반증하는 유일한 축」이 **한 번도 실행되지 않은 채 4게이트가 전부
  녹색**이었다. 오케스트레이터가 그 명령을 직접 실행해 재현했다.
- **SP-5가 못 잡은 이유가 기록된 한계보다 한 겹 더 나쁘다.** SP-5는
  `allText.includes("node scripts/verify-evidence.mjs")` **부분 문자열**만 봤다. 게이트가 적어 둔
  한계는 「명령을 적어 두고 실행하지 말라고 덧붙이면 통과」였는데, 실제로 뚫린 것은 그보다 앞이다 —
  **실행조차 불가능한 명령**도 통과했다.
- **콜드 리뷰 6건을 전부 닫았다.** major 2(SKILL.md `--identity` / `updateRegistry` 비보호 예외),
  minor 3(`project-ledger --out` 계약 밖 exit 1 / SP-6 파일명 하드코딩 / AC-39 공허한 서술),
  suggestion 1(`checkStorageBoundary` 심볼릭 링크 한계 미문서화).
- **수정본 자체에 적대적 리뷰를 돌려 2건을 더 닫았다.** `writeJsonAtomic`(388행, major)과
  `loadSchema`(360행, minor)가 **같은 파일 안에서** 여전히 비보호였다 — 내가 `updateRegistry`
  주석에 「플러그인 설치가 손상돼 스키마를 읽지 못하는 경우」라고 직접 써 놓고 두 줄 위의 같은
  시나리오를 열어 둔 형태다. 둘 다 실행으로 재현한 뒤 고쳤다(ENOTDIR / ENOENT 원시 스택 + exit 1,
  통제군은 exit 0).
- **자기 리뷰의 반증 1건도 버리지 않았다.** 「WA-25의 300자 경로 트리거가 Linux에서 공허하게
  통과한다」는 major 주장이 실제 WSL2 실측으로 반증됐지만, 그 과정에서 **비공허성이 암묵적 전제에
  기댄다**는 것이 드러나 WA-25가 새 가드만 쓰는 사유 코드(`REGISTRY_UNEXPECTED_ERROR`)를 요구하도록
  좁혔다. 이제 기존 early-return으로 빠져도 통과하지 않는다.
- **신규 단언 12건**(433 → 445). SP-6a·6b·6c(SP-6 대체, +2) · SP-9 · SP-10 · LP-11 ·
  WA-25 · WA-26 · WA-27 · WA-28 · DH-1a·1b·1c. **12건 전부 수정 전에 FAIL하는 것을 먼저 관측한 뒤**
  고쳤다(허용 방향 WA-26과 서술만 바뀐 AC-39는 그대로 PASS).
- **SP-10은 SP 계열 중 유일하게 소스 스캔이 아니다.** 프롬프트에서 인용 검증 명령을 추출해
  자리표시자를 고정 표로 치환한 뒤 **실제로 실행**하고, 인자 검증을 통과했다는 증거
  (`[verify-evidence] citations:` 보고 줄)를 확인한다. 치환표에 없는 자리표시자가 남으면 FAIL이다.
- **변이 15종을 격리 사본에서 병렬 관측했고 15/15가 기대 반경과 일치했다**(불일치 0).
  1회차 10종(M1~M10) + 2회차 5종(M11·M12·M13·M3b·M8b). 워킹 트리에 변이가 남지 않은 것을
  변경 파일 목록과 흔적 검색으로 확인했다.
- **M9가 AC-39 서술 정정의 근거다.** `locked: prevNode.locked === true`를 리터럴 `false`로 바꿔도
  **FAIL 0건**이다(440/0 실측). 규칙 1의 early-return이 그 자리에서 `prevNode.locked !== true`를
  이미 보장하므로 도달 가능한 모든 경로에서 `false`와 동치다. **그래서 코드는 고치지 않고 주석만
  정정했다** — 규칙 1이 나중에 옮겨지면 그 줄이 곧바로 짐을 지는 방어적 중복이다.
- **M3b가 원래 결함 상태를 재현한다.** `--identity` 줄을 다시 지우면 SP-9·SP-10이 FAIL한다.
  이전에는 같은 상태에서 4게이트가 전부 녹색이었다.
- **문서 위치를 재편했다(`1e9f31d`).** `docs/harness/`를 gitignore하고, 프로젝트 기억 7건을
  `docs/devcareer-prep-plugin/`으로, 핸드오프 6건을 `docs/handoff/`로 승격했다. 경로 참조
  **65건**을 따라 고쳤다(`SPEC_MD_REL` + `run-smoke.mjs`가 세그먼트로 조립하던 3곳 포함).
- **`spec.md`가 옮겨야 했던 이유를 실측했다.** 클론에서 `spec.md`를 지우면 **4건 FAIL**이다 —
  R-8 배지 드리프트, samplingMethod 드리프트, 섹션 예외 중단, 그리고 **AC-1(정상 레포 → exit 0)**.
  드리프트 가드가 lint에도 배선돼 있어 **`npm run lint` 자체가 깨진다.** ignore만 하고 파일을
  두었다면 워킹 트리는 계속 녹색이고 새 클론에서만 게이트 2개가 죽었다.
- **DH-1 가드의 닻이 `.gitignore`다.** 처음엔 금지 접두사를 테스트에 박았고 **가드가 자기 자신을
  잡았다**. 거기서 「이 파일만 예외」를 두는 것이 이 레포가 계속 닫아 온 자기면제 통로라서 닻을
  정본(`.gitignore`)으로 옮겼다. 추적 `.mjs`에 무시 경로 주석 한 줄만 넣어도 DH-1b가 FAIL하는 것을
  실측했다(444/1).
- **PII 1건을 가리고 푸시했다(`95c4a61`).** `cold_review.md`의 A-16 재현 기록에 실제 회사 도메인
  주소가 있었다. 사용자 확인을 받아 `dev@Example.com`으로 바꿨고, 「실측은 본인 이메일로 했고 주소만
  가렸다」를 함께 적어 픽스처를 원래 관측인 것처럼 제시하지 않았다. 그 발견의 요지는 「도메인
  대소문자 하나로 total=0」이므로 예제 도메인으로도 보존된다.
- **푸시 전 전수 스캔했다** — 로컬 절대경로 0건, `.devcareer/` 미추적, 사용자 개인 이메일
  (`jjunggulee`) 0건, 그 외 주소는 전부 `example.*`/`*.test` 픽스처. 남은 실주소는 플러그인
  메타데이터의 본인 GitHub noreply 하나다.

## In Progress

**`.harness/state.json`이 없다 — 결함이 아니라 사용자 결정이다.** 이 프로젝트는 `/harness` 상태
기계 밖에서 진행한다. 따라서 `Skill`/`Task`/`Phase`/`Mode`/`Docs` 고정 라벨 기록이 이 문서에 없고,
`/handoff resume`의 Step 3.5는 축소 검사로 "legacy handoff — task state not machine-verifiable"을
보고한다. 그것이 정상이다.

이 세션에서 진행 중이던 것은 없다. 다섯 회차를 각각 커밋하고, 4게이트 녹색 + 새 클론 검증 +
원격 동기(0/0) 상태에서 정지했다.

## Blockers / Risks

- **`runSamplingLiteralDriftSmoke`의 관측 장치가 고장나 있다 — 이번 회차에 실측했고 고치지
  않았다.** `spec.md`가 없으면 각 단언이 FAIL하는 대신 섹션 전체가 `copyFileSync` 예외로 중단된다.
  이 레포의 완료 조건이 **「변이가 섹션을 예외로 중단시키면 관측 장치가 고장난 것이다 — 파일·필드
  부재를 예외가 아니라 각 단언의 FAIL로 떨어뜨려라」**를 명시하므로 규칙 위반이다. 5줄 정도면
  닫힌다. 현재 커밋 상태의 게이트에는 영향이 없다.
- **f029375 콜드 리뷰 미반영은 11건 → 10건이다.** 이번에 확실히 닫힌 것은
  **(Maintainability #4) `project-ledger.mjs`의 `--out` 오류 처리** 하나다. WA-25·WA-26이
  **(Major #3) exit 4 분기 미관측**의 일부를 덮지만 그 발견이 요구한 **CLI 레벨 관측은 portable하게
  만들 수 없어** 함수 계약(「이 함수는 던지지 않는다」)으로 대체했으므로 **완전히 닫혔다고 보지
  마라**. 남은 것:
  - (Architecture) prev 유래 노드의 스키마 위반이 exit 1로 나가 잘못된 복구 절차를 지시한다.
  - (Architecture) `state.json` 레지스트리 read-modify-write에 동시성 제어가 없다.
  - (Maintainability) 드리프트 가드가 없는 사본 3건 — `EMPTY_REGISTRY_ARTIFACTS`,
    `STATE_SCHEMA_VERSION`, `EVIDENCE_FILE_NAME`.
  - (Maintainability) evidence contentHash가 `content-hash.mjs`와 이중 구현.
  - (Testing) `PREV_ARTIFACT_HASH_MISSING` 보류 사유 미검증.
  - suggestion 잔여 3건.
  **전량 리포트는 `docs/harness/f029375/review_report.md`이고 이제 추적되지 않는다** — 새 클론에는
  없다. 위 목록이 클론에 남는 유일한 백로그다.
- **`config.json` 쓰기 주체가 없다.** `writeConfig`의 프로덕션 호출자가 0건이므로 SKILL.md 0단계의
  「확정 결과를 설정 파일에 저장한다」는 실행 경로가 없다. 7단계는 `--identity`로 우회한다.
  콜드 리뷰 major #1의 나머지 절반이며 범위 확정 대화 구현과 함께 닫힌다.
- **SP-10은 인용 검증 호출 하나만 실행한다.** 나머지 단계(1·2·4·6·8·9)의 인자 완비성은 여전히
  미관측이다 — 7단계와 같은 결함이 다른 단계에 있어도 지금은 아무것도 빨개지지 않는다. 전부
  돌리려면 사실상 프롬프트에서 파생한 엔드투엔드 스위트가 필요하다.
- **SP 계열은 여전히 보조 방어다.** SP-10을 뺀 나머지는 소스 스캔이므로 프롬프트가 명령을 부르고
  **결과를 무시하는** 경로는 남아 있다. 「실제로 호출했는가」는 계측 없이 관측할 수 없다.
- **템플릿 역할 선언은 자기 선언이다.** 템플릿이 스스로 「판정」이라고 적어 생성 쪽 요구를 피할 수
  있다. 대가는 즉시 나온다(쓰기 경계가 exit 1로 거부) — 그러나 집행은 그쪽이지 이 축이 아니다.
- **경로 경계가 막지 못하는 것.** `.devcareer` 세그먼트만 보므로 경계 **안에서의 오배치**는 통과하고,
  그 세그먼트가 **심볼릭 링크·정션**이어도 통과한다(이번에 문서화했다). 막는 것은 적대적 주체가
  아니라 오케스트레이션이 조립한 잘못된 문자열이다.
- **C-5 판정이 산출물 단위다** — 노드 99개가 `insufficient`이고 1개만 external이면 집행 1건으로
  PASS가 된다. 노드 단위 커버리지는 AC-13 배지 몫이며 구현 8단계에서 다시 봐야 한다.
- **콜드 리뷰 T4 14건은 슬라이스 C로 이연 확정**(`a228329`). **예외를 앞당겨 받는 유일한 조건은 그
  항목이 그 회차의 작업을 실제로 막을 때**다.
- **도그푸딩 대상 레포가 미확정이다 — 사용자 결정이 필요하다.** 200커밋 이상·다중 저자·한글 커밋
  메시지가 있는 실제 레포. **이 단계가 타인 PII가 픽스처가 아니라 실물로 처음 흐르는 지점이다.**
- **(f)축의 잔여 위험은 그대로다** — allow-list 대조는 URL이 **목록 소속인지만** 확인한다.
- **스펙에만 적히고 코드는 없는 항목** — C-1(오염 스위트 실행 모델), m-1, m-2.
- **미검사로 남은 영역** — `plan_critic_findings.md`의 「미검사 영역」 표. **프롬프트 계층은 이제
  콜드 리뷰 1회를 받았다**(직전 회차 0회에서). 구현 8~12단계는 여전히 0회다.
- **브랜치가 public 원격에 있다.** 이후 커밋은 즉시 공개된다 — PII·절대경로 스캔 없이 푸시하지 마라.
  `main` 병합·PR은 사용자 지시가 없어 하지 않았다.

## Next Steps

1. **`runSamplingLiteralDriftSmoke`의 섹션 예외 중단을 고쳐라.** `spec.md` 부재가 각 단언의 FAIL로
   떨어지게 하고(`copyFileSync`를 감싸거나 부재를 먼저 판정), 그 상태를 변이로 관측하라 — 이 레포의
   완료 조건이 요구하는 형태다. **실행 가능한 슬래시 명령은 없다**; 5줄 규모의 직접 수정이다.
2. **구현 7단계 잔여 — 범위 확정 대화의 실제 구현.** SKILL.md 0단계가 절차를 적었지만
   `git shortlog -sne` 파싱 → 저자 다중 선택 UI → `config.json` 저장의 **실행 경로가 아직 없다.**
   `schemas/config.schema.json`의 8개 required 필드를 채우는 코드가 어디에도 없으므로 **설정 파일을
   쓰는 주체를 먼저 정하라**(스크립트인가 스킬인가 — 쓰기 경계 논리와 같은 질문이다). 정하면
   SKILL.md 7단계가 `--config`를 쓸 수 있게 되고 콜드 리뷰 major #1의 나머지 절반이 닫힌다.
   편집 병합의 사용자 대면 절차(exit 3 → 보여 줄 문구)도 여기 붙는다.
3. **구현 8단계 — `skills/skill-gap/`.** **여기서 (f)축이 처음으로 대상을 갖는다.** 구현 8단계
   (d)의 「L2·L3에서 `verification`이 무엇을 반증하는가」 정의를 프롬프트에 실제로 반영해야 잔여
   위험이 줄어든다. 렌더러·쓰기 경계 모두 계층 중립이므로 `LAYER_TITLES`와 진입점만 늘리면 된다.
   **SP 계열 오라클은 `skills/**` 전체를 스캔하므로 새 스킬에 자동으로 적용된다** — 새 템플릿이
   역할 선언(SP-6a)·티어(SP-7)·출력 언어(SP-8)를 안 적으면 바로 FAIL한다. 새 템플릿이 자기 검증
   호출을 들고 오면 SP-9·SP-10도 그것을 함께 검사한다(전 블록 순회로 일반화해 뒀다).
4. **구현 9단계 착수 전에 C-1(오염 스위트 실행 모델)을 스펙에 명문화하라.** 스킬 실행 3회는
   사람이 수행해 산출물을 `tests/contamination/runs/<run-id>/`에 남기고 `--contamination`은 채점만
   한다.
5. **구현 10단계 착수 전에 도그푸딩 대상 레포를 확정해 `spec.md`에 기재하라.**

## Definition of Done

**에픽 전체**: `slice_plan.md`의 3슬라이스가 모두 완료되고, 사용자 본인 Git 히스토리로 생성한 경력
기술서가 "이 정도면 실제로 쓸 수 있다" 수준이며, 갭 분석이 "공감되고 우선순위가 명확하다"고 느껴지고,
생성된 코테 문제가 실제 경력과 논리적으로 연결되며, 할루시네이션으로 인한 잘못된 지식 설명이 거의
없는 상태.

**슬라이스 B 단독**: `/devcareer-prep:career-from-git`과 `/devcareer-prep:skill-gap`이 동작하고,
오염 주입 스위트 40건이 `AC-8` 기준을 만족하며, 4게이트가 녹색인 상태.

**이 세션이 유지·추가한 완료 조건**:
- 새 제약을 넣을 때마다 **그 절이 실제로 FAIL을 내는 것을 절 단위로 관측한다.**
- **완화(제약을 넓히는 변경)는 허용 방향도 관측한다.**
- **금지 방향 단언 앞에 「대상이 존재하는가」를 먼저 단언하라.** 대상이 0건이면 공허하게 통과한다.
- **상태를 늘렸으면 그 상태 값 자체를 단언하라.**
- **계약 검사는 그 픽스처가 스키마를 실제로 통과하는지 함께 단언한다.**
- **리터럴 드리프트 가드의 닻은 그 리터럴을 정의한 모듈 밖에 둔다.**
- **변이가 섹션을 예외로 중단시키면 관측 장치가 고장난 것이다.** 부재를 예외가 아니라 각 단언의
  FAIL로 떨어뜨려라. **(이번에 위반 1건을 실측했다 — Next Steps 1)**
- **소스 스캔 단언은 「이름이 등장하는가」가 아니라 「호출 지점이 있는가」를 물어라.**
- **(신규) 「호출 지점이 있는가」로도 부족하다 — 실행 가능한가를 물어라.** SP-5가 부분 문자열만
  보아 실행조차 불가능한 명령을 통과시켰다. 가능한 축에서는 **실제로 실행**하고 그 단계가 돌았다는
  증거를 요구하라(SP-10).
- **(신규) 오라클의 대상 선정을 파일명으로 하드코딩하지 마라.** 새 대상만 조용히 빠진다.
  의미 기준(역할 선언)으로 분류하고 역할별로 다른 것을 물어라.
- **(신규) 단언이 「어느 경로로 실패했는가」를 고정하게 하라.** 반환값 모양만 보면 겨냥한 새 코드가
  한 번도 실행되지 않아도 통과한다(WA-25의 사유 코드 요구).
- **(신규) 예외를 반환값으로 바꿀 때는 호출 지점이 아니라 함수 안에 두어라.** export된 함수의
  호출 지점만 감싸면 「어떤 실패는 `{ok:false}`, 어떤 실패는 예외」라는 두 얼굴의 계약이 남는다.
- **(신규) 종료 코드는 「호출자가 취할 조치」로 갈라라.** 산출물을 쓰기 **전** 실패는 exit 4가 될 수
  없다 — 4는 「기록됐다」를 뜻한다.
- **(신규) 각 커밋이 그 시점에 녹색인지 확인하라.** 최종 상태만 녹색이면 bisect가 무의미하다.
- **(신규) gitignore된 경로를 추적 코드가 참조하지 못하게 하라.** 워킹 트리는 녹색이고 새 클론에서만
  깨지는 고장이 난다(DH-1). **닻은 `.gitignore`에 두어라** — 테스트에 리터럴을 박으면 가드가 자기
  자신을 잡고, 그것을 피하려는 예외가 자기면제 통로가 된다.
- **(신규) 클론 안전성은 실제로 클론해서 확인하라.** 로컬 통과는 근거가 아니다.
- **에이전트 리포트를 그대로 받지 마라.** 이번 회차도 major 2건·minor 1건과 골든 게이트 의문을
  전부 직접 실행해 재현한 뒤 반영했다.

## Reading Order

1. `docs/handoff/2026-08-20-slice-b-coldreview-closed-docs-moved.md` — 이 문서. 현재 위치와 남은 것.
2. `docs/devcareer-prep-plugin/conventions.md` — 규약 문서. **§9(신규)가 문서 위치 규약과 이번
   재편이 바꾼 도구 동작 2건을 적고 있다.** 이 레포엔 `CLAUDE.md`가 없다 — 승격을 검토하라.
3. `skills/career-from-git/SKILL.md` — 오케스트레이션 절차 9단계. 7단계의 `--identity`와 exit 2
   양의성 경고가 이번에 추가됐다. 「이 스킬이 지켜야 하는 것」 4개 항이 집행 코드와 1:1 대응한다.
4. `docs/devcareer-prep-plugin/slice_b_spec_review.md` — 뒤쪽 **'착수 전 게이트 체크리스트'**.
   **게이트 A·B·C·E가 전부 `[x]`이고 D(도그푸딩)만 열려 있다.** E-4 항목에 **「거짓 전제 위에서
   닫혀 있었다」는 정정**이 적혀 있다 — 그 문단을 반드시 읽어라.
5. `scripts/write-artifact.mjs` — 쓰기 경계. 파일 헤더의 **5분기 종료 코드 계약**(exit 3의 다섯 가지
   `[HOLD]` 사유가 이번에 정리됐다)과 `updateRegistry`의 「이 함수는 던지지 않는다」 계약.
6. `tests/run-smoke.mjs`의 `runSkillPromptContractSmoke` — SP-1~SP-10. **SP-10만 소스 스캔이 아니다.**
   절 주석이 왜 이 축이 보조 방어인지와 남는 구멍을 적고 있다. 6400줄이 넘으므로 함수 단위로 잘라
   읽어라(`runIgnoredPathReferenceSmoke`도 함께 볼 것 — DH-1).
7. `scripts/lib/artifact-contract.mjs` — 계약 정본. `mergeArtifact` 규칙 4의 **정정된 주석**
   (「이 줄이 잠금을 이어받는다」고 읽지 말 것 — 변이 M9가 실측한 형태).
8. `scripts/lib/store.mjs`의 `checkStorageBoundary` — 경로 경계. **「이것이 막지 못하는 것」 목록에
   심볼릭 링크·정션이 이번에 추가됐다.**
9. `docs/devcareer-prep-plugin/spec.md` — 실행 스펙 정본(22 AC / 12 구현단계). **131KB이므로
   통독하지 말고** `awk '/^\*\*8\. /,/^\*\*9\. /'` 같은 방식으로 필요한 단계만 잘라 읽어라.
   **이 파일은 산문이 아니라 드리프트 가드 2건이 기계로 읽는 닻이다.**
10. `docs/devcareer-prep-plugin/slice_plan.md` — 3슬라이스 분할, 슬라이스 A 파일 수정 예외 5건
    (전부 소비됨), **T4 14건의 슬라이스 C 이연 결정**.
11. `docs/devcareer-prep-plugin/plan_critic_findings.md` — 「미검사 영역」 표. 다음 회차 입력.

## Do NOT

- **`/harness`를 쓰지 마라 — 이 프로젝트는 상태 기계 밖에서 진행한다(사용자 결정).**
- **`main`에 병합하거나 PR을 열지 마라** — 사용자 지시가 없었다. 브랜치만 푸시된 상태다.
- **PII·절대경로 스캔 없이 푸시하지 마라.** public 레포이고, 한번 올라가면 인덱싱·캐시되어 되돌릴
  수 없다. 이번에 실주소 1건을 그렇게 잡았다.
- **`docs/harness/` 아래를 추적되는 코드·lint 스캔 대상이 참조하게 하지 마라.** DH-1이 막는다.
  금지 접두사의 정본은 `.gitignore`다 — 테스트에 리터럴로 박지 마라.
- **`spec.md`를 옮기거나 이름을 바꿀 때 `SPEC_MD_REL`과 `run-smoke.mjs`의 경로 조립 지점을 빠뜨리지
  마라.** 빠뜨리면 워킹 트리는 녹색이고 새 클론에서 `npm run lint`까지 죽는다(실측 4 FAIL).
- **리뷰 리포트를 `docs/`로 승격하지 마라** — `/deep-review`의 라운드 자동 감지가
  `docs/harness/<slug>/`를 스캔하므로 같은 대상을 라운드 1로 되돌린다.
- **`/handoff resume`에 경로를 직접 줘라.** 경로 없는 `resume`과 `list`는 `docs/harness/handoff/`를
  보므로 승격된 핸드오프를 찾지 못한다. `generate`도 그 아래에 쓰므로 남길 것은 `docs/handoff/`로
  옮겨 커밋하라(이 문서는 그렇게 만들어졌다).
- **`slice_plan.md`의 예외 5건 밖에서 슬라이스 A 파일을 수정하지 마라. 5건은 모두 소비됐다.**
  T4 나머지는 슬라이스 C로 이연됐다 — **그 항목이 그 회차 작업을 실제로 막을 때만** 예외를 받아라.
- **`review_report.md`를 수정하지 마라** — 라운드 기록이다. 같은 대상 재실행이 라운드 2를 만든다.
- **`skills/**` 안에 원장 원본 파일명 리터럴을 쓰지 마라.** SP-3이 막는다.
- **생성 출력이 `verification`이나 `locked`를 담게 하지 마라.** 값이 아니라 **필드의 존재**가
  금지다. 둘 다 병합이 채운다. **`locked` 금지를 단계나 계층으로 완화하지 마라.**
- **템플릿의 역할 선언(`역할: 생성`/`역할: 판정`)을 지우지 마라.** 지우면 그 파일이 어느 요구도 받지
  않는 사각지대가 된다(SP-6a가 막는다).
- **`--stage` 자기 선언을 프롬프트 문구로 막았다고 생각하지 마라.** 반증하는 유일한 축은 인용
  무결성이고, 그 호출에 **`--identity`나 `--config`가 반드시 있어야** 축이 실행된다.
- **`verify-evidence.mjs`의 exit 2를 INCONCLUSIVE로 단정하지 마라.** `[INPUT_ERROR]`도 같은 코드다.
  접두사로 구별하라 — 「증거 부족」과 「인자 실수」는 사용자에게 전혀 다른 이야기다.
- **exit 3을 `--force`로 자동 재시도하지 마라.** 게다가 `ARTIFACT_WRITE_FAILED`·
  `LAYER_SCHEMA_UNREADABLE`은 `--force`로 해소되지 않는다 — `[HOLD]` 코드를 보고 구별하라.
- **산출물을 쓰기 전 실패를 exit 4로 보고하지 마라.** 4는 「기록됐다」를 뜻한다.
- **`nodes` 비배열을 빈 배열로 강등하지 마라.** **읽기 실패를 '파일 있음'으로 단정하지 마라.**
- **`mergeArtifact` 규칙 4의 `locked` 표현식을 리터럴 `false`로 단순화하지 마라.** 규칙 1의
  early-return이 옮겨지면 그 줄이 짐을 진다(방어적 중복). 대신 주석이 무엇을 관측하는지 정확히 적어라.
- **경로 경계에 테스트용 우회(환경변수·`--allow-root`)를 추가하지 마라.** 오케스트레이션이 조립할 수
  있어 자기면제 통로가 된다. 테스트 루트가 불변식을 만족하게 하라.
- **변이 하네스를 워킹 트리에서 돌리지 마라.** 격리 사본(`cp -r`)에서 돌리고, 도구 타임아웃보다
  길게 묶지 마라 — `finally`가 돌기 전에 죽으면 변이가 워킹 트리에 남는다(과거 두 번 났다).
- **새 검사를 넣고 "영역당 한 번" 변이로 관측했다고 보고하지 마라.** 절 단위로 하나씩 지워보고
  **대응하는 단언만** FAIL하는지 확인하라.
- **기존 단언이 새 변경을 반증하면 그 단언을 고쳐 맞추지 마라 — 변경을 좁혀라.**
- **단언을 전체 위반 수(`length === 0`)로 쓰지 마라.** 겨냥한 경로의 위반만 보라.
- **README에서 미구현 경로를 백틱으로 적지 마라** — `DOC_PATH_NOT_FOUND`가 FAIL을 낸다.
  실재 경로는 백틱, 계획된 경로는 이탤릭이다.
- **배지를 `basis`에서 파생시키지 마라.** **배지·`samplingMethod` 리터럴을 한 곳만 고치지 마라**
  (각각 4곳이 드리프트 가드로 묶여 있고 그중 하나가 `spec.md`다).
- **`redact.mjs`·`schema-validate.mjs`·`lang-lint.mjs`·`validate-plugin.mjs`를 수정하지 마라** —
  예외 목록에 없다.
- **`--secret-scan`의 면제를 필드 단위로 넓히지 마라.** **allow-list 대조를 문자열 prefix로 바꾸지
  마라.** **조건부 제약을 최상위 `if/then`이나 `anyOf`로 쓰지 마라**(관례는 `allOf` 원소).
- **`excluded` 커밋의 원장 전량 등재를 되돌리지 마라. T3의 PII 3필드 축소도 되돌리지 마라.**
- **`fixtures/golden/sampling-300.expected.json`을 수집기 출력으로 덮어쓰지 마라.**
- **"현재 픽스처에서 문제가 안 난다"를 회귀 없음의 근거로 쓰지 마라. 픽스처를 세계로 착각하지 마라.**
- **자기충족 테스트를 만들지 마라.** 범용 오류 코드를 쓰는 케이스는 위반 메시지 조각까지 단언하라.
- **도그푸딩 대상 레포 이름을 지어내지 마라.** 미확정이며 사용자가 정해야 한다.
- `LICENSE`(MIT)와 `.gitignore`의 기존 항목을 수정하지 마라. `package.json`의 `npm test` 배선을
  되돌리지 마라.
- **PowerShell here-string(`@'...'@`)을 Bash 도구에 쓰지 마라.** 긴 커밋 메시지는 파일에 쓰고
  `git commit -F <file>`로 넘겨라(`-F`와 `-m`은 함께 못 쓴다 — 서명 줄도 파일에 이어 붙여라).
- **Bash 도구의 heredoc에 백슬래시를 그대로 쓰지 마라.** 정규식 리터럴은 조립하라.
- **`git apply`용 패치를 손으로 만들 때 끝 개행을 빠뜨리지 마라** — "corrupt patch"가 난다.
  헝크를 골라 커밋을 나눌 때는 **최종 파일에서 뒷 그룹을 역적용**해 중간 버전을 만들어라(오프셋이
  어긋나지 않는다).

## Progress Ledger

| Epic | Slice | Status | Evidence | Notes |
|------|-------|--------|----------|-------|
| devcareer-prep-plugin | slice-a-deterministic-foundation | done | 97675e7b1a9204aa722584d9917d9cde07db89de | 구현 1~6단계 + 콜드 리뷰 T1·T2·P8 반영. 4게이트 녹색(lint 0 / 201 / 19 / 11). T3·T4 미반영. 원격 미푸시. |
| devcareer-prep-plugin | slice-b-gate-a-t3-prework | done | 299315b87a9cb827cb7861210debc0a3b4cc5750 | 슬라이스가 아니라 슬라이스 B **착수 전** 스펙·계약 개정. 심사 16건 → 게이트 A·B + T3 반영 → 적대 검증 4렌즈. 4게이트 녹색(lint 0 / 257 / 23 / 11). **위 slice-a 행의 Notes는 그 시점 기록이라 T3 미반영으로 남아 있으나 이 행 이후 T3는 반영됐다** — 근거 커밋만 보고 현재 트리를 판단하지 마라. |
| devcareer-prep-plugin | slice-b-p0-skill-layer | in-progress | 95c4a619dd3a435c8142fcccaad41fa968599507 | **구현 7단계 완료 + 프롬프트 계층 콜드 리뷰 1회차 8건 닫힘 + 문서 위치 재편 + 원격 첫 푸시.** 이전 회차들이 예외 1~5·렌더 계약·진입점 3종·게이트 B-7·E-3·E-4를 닫았고, **이 세션이 게이트 E-4의 거짓 전제(7단계 명령에 `--identity`가 없어 인용 검증 축이 한 번도 실행되지 않았음)를 발견해 닫고, SP-9·SP-10을 신설했다.** 콜드 리뷰 6건 + 자기 리뷰 2건 반영, 반증 탈락 0건. 신규 단언 12건(433→**445**), 변이 15종 15/15 일치. **`docs/harness/` gitignore + 프로젝트 문서 7건·핸드오프 6건 `docs/` 승격, 경로 참조 65건 갱신, DH-1 가드 신설.** 4게이트 녹색을 **새 클론에서도 확인**(lint 0 / 445 / 33 / 11). PII 1건 가림. `origin/harness/devcareer-prep-plugin-2`와 동기(0/0), `main` 미병합(35커밋 앞섬). **미착수: 범위 확정 대화 실행 경로(`config.json` 쓰기 주체 없음) · 구현 8~10단계 · `runSamplingLiteralDriftSmoke` 섹션 예외 중단(규칙 위반, 실측됨).** **f029375 미반영 11→10건.** 게이트 D(도그푸딩)만 열려 있음. T4 14건은 슬라이스 C 이연. |

## Resume
Run: `/handoff resume docs/handoff/2026-08-20-slice-b-coldreview-closed-docs-moved.md`
