# HANDOFF — 슬라이스 B: 프롬프트 계층 + 게이트 B-7·E-3·E-4 닫힘

**Date:** 2026-08-20  **Project:** career-forge (devcareer-prep 플러그인)
**Branch:** harness/devcareer-prep-plugin-2  **HEAD:** e4e5a51345763143a36b666751ba42ab57c8beef — docs(readme): correct the status table the prompt layer just made false
**Dirty:** clean  **Upstream:** 없음 (원격 `origin` = github.com/Jugger0716/career-forge. 이 브랜치가 원격 `main`보다 **30커밋** 앞섬 — 푸시하지 않음)

## Goal

개발자의 Git 히스토리를 분석해 경력 기술서 → 지식맵 → 갭 리포트 → 학습·코테 계획을 만드는
Claude Code 플러그인(`devcareer-prep`)을 구축한다. 핵심 가치는 **할루시네이션 방지** — 모든 사실적
주장이 실존하는 커밋 해시나 신뢰 가능한 출처로 뒷받침되어야 하고, 그 검증을 LLM 판단이 아니라
**결정적 스크립트**가 수행한다.

에픽은 `foundation-first` 3슬라이스다. ①은 완료됐고 ②가 진행 중이다. 직전 세션이 결정적 진입점
3종을 세우고 콜드 리뷰 3건을 반영했고, **이 세션은 그 위에 프롬프트 계층(스킬 1종 + 템플릿 2종)을
올리고 열려 있던 게이트 3건(B-7 · E-3 나머지 절반 · `--stage` 자기 선언)을 전부 닫았다.**

## Current State (verified)

- **커밋 4개** — `0b44f24`(게이트 B-7) → `a1c85fb`(콜드 리뷰 4건) → `4a31b33`(프롬프트 계층) →
  `e4e5a51`(README 정정). `git log --oneline 7e88aa6..HEAD`로 확인. 워킹 트리 **clean**. 원격 미푸시.
- **4게이트 녹색** — `npm run lint` exit 0 / 스모크 **433 PASS** / `--negative` **33 PASS** /
  `--golden` **11 PASS**. 오케스트레이터가 직접 실행해 확인(세션 시작 시점은 0/402/27/11).
- **`skills/career-from-git/`이 생겼다** — `SKILL.md`(오케스트레이션 절차 9단계) +
  `templates/career-writer.md`(중간 티어) + `templates/fact-checker.md`(상위 티어). **슬라이스 A
  프로덕션 파일은 하나도 수정하지 않았다** — 새 예외를 받지 않았다.
- **게이트 B-7 닫힘(`0b44f24`).** `locked`를 생성 출력이 담을 수 없게 했다 — 값 제약이 아니라
  **필드 존재 금지**이고, 단계(`draft`/`fact-checked`)로도 계층(`plan` 포함)으로도 완화되지 않는다.
  병합이 채운다(기존 노드는 prev 값, 신규 노드는 `false`). **잠금이 생기는 경로는 사용자가 산출물
  파일을 직접 편집하는 것 하나만 남았다.** 관측 AC-34~AC-42 · WA-18 · WA-19.
- **B-7 변이 6종을 실측했다.** B1(절 무력화)→5건, B2(값 기준으로 좁힘)→AC-35만,
  B3(draft만 검사)→AC-36·WA-18, B4(verification 축 안쪽으로 이동)→AC-37만,
  B5(병합 이어받기 제거)→AC-39·AC-41·WA-10·WA-17, B6(신규 노드 채우기 제거)→19건.
  **B5·B6의 넓은 반경은 관측 결함이 아니다** — 스키마가 `locked`를 required로 두므로 병합이
  안 채우면 모든 쓰기가 실제로 막힌다.
- **B-7 관측이 픽스처 자체의 결함을 잡아냈다.** 헬퍼가 `locked: true` 오버라이드를 **조용히
  버리고 있었고**, WA-16이 잠긴 prev 노드를 심으려다 아무것도 못 심은 채 녹색이었다. 헬퍼가
  이제 그 오버라이드에 대해 **던진다**. 잠금을 심으려면 파일을 직접 편집해야 한다.
- **콜드 리뷰 4건 반영(`a1c85fb`) — 사용자가 고른 범위 그대로다.** Security #11(경로 경계) ·
  Correctness #7(`--force` 경고) · Correctness #9(EACCES 오분류) · Testing #3(`--root`/`--out`).
  **11건은 미반영으로 남았다.**
- **Security #11에 테스트용 우회 수단을 만들지 않았다.** 리뷰는 환경변수나 `--allow-root`를
  함께 설계하라고 했지만 그 값은 오케스트레이션이 조립할 수 있어 자기면제 통로가 된다. 대신
  **스모크 임시 루트가 진짜 불변식을 만족하게** 고쳤다(`…/<tag>/.devcareer`). 판정은
  `resolveStorageRoot` 일치가 아니라 **경로 세그먼트 정확 일치**다(부분 일치 `.devcareer-old`는
  거부 — 변이 S2가 그 단언만 깬다).
- **Correctness #9의 「exit 1 위장」을 직접 재현했다.** `career.json` 자리에 디렉터리를 두고
  `--force`로 부르면 — **수정 전**: exit **1** + 원시 `node:fs binding.copyFile` 스택(문서화된
  exit 1 「출력을 고쳐 다시 부른다」로 위장), **수정 후**: exit **3** +
  `[HOLD] PREV_ARTIFACT_BACKUP_FAILED … (EPERM)`. 멈추는 지점이 `writeJsonAtomic`보다 앞이라
  「쓰지 않았다」 불변식은 유지된다.
- **변이 S6이 WA-24의 존재 이유를 실측했다.** `existence`를 `"unknown"`→`"present"`로 되돌려도
  WA-22·WA-23은 **둘 다 녹색**이다(양쪽 다 `!== "absent"`라 강행 분기가 같다). WA-24(반환값
  직접 대조)가 없었으면 3상태가 **이름만 셋인 2상태**였다.
- **게이트 E-3 닫힘(`4a31b33`) — 양방향 관측.** (SP-2) 프롬프트가
  `node scripts/project-ledger.mjs` **호출 지점**을 담는가, (SP-3) 원장 원본 파일명 리터럴을
  **담지 않는가**. SP-3의 닻은 하드코딩 문자열이 아니라 정본 상수 `EVIDENCE_FILE_NAME`이다.
- **변이 P8이 SP-1(대상 존재)의 필요성을 실측했다.** `skills/`를 통째로 감추면 SP-1·SP-2·
  SP-4~SP-8 **7건이 FAIL하는데 SP-3(금지 방향)만 녹색으로 남는다** — 대상 0건이면 금지 단언은
  공허하게 통과한다. **게이트 E-3이 「절반 열려 있다」고 기록했던 상태가 정확히 그것이다.**
- **프롬프트 변이 P1~P7이 각각 한 단언만 깼다.** P1(호출 지점→이름 언급)→SP-2, P2(원장 파일명
  삽입)→SP-3, P3(검증 호출 제거)→SP-5, P4(쓰기 경계 호출 제거)→SP-4, P5(locked 금지 문구
  제거)→SP-6, P6(세션 모델 문구 제거)→SP-7, P7(출력 언어 명시 제거)→SP-8.
- **게이트 E-4 신설·닫힘 — `--stage` 자기 선언.** 쓰기 경계에서는 막지 못한다는 결론을 그대로
  기록했다. **판정 로그 파일을 요구하는 안은 기각**했다(그 파일을 만드는 주체도 같은
  오케스트레이션이라 위조가 한 단계 늘 뿐이다). 실제 닻은 인용 무결성이고 **그것은 이미
  있었다** — 없던 것은 프롬프트가 그것을 반드시 거치게 하는 배선이다. SKILL.md 7단계가 그
  호출을 넣고 (SP-5)가 관측한다.
- **휴면 검사 6종이 처음으로 대상을 갖고 각자의 코드로 FAIL하는 것을 실측했다** —
  `SKILL_MD_NOT_FOUND` · `SKILL_FRONTMATTER_MISSING` · `SKILL_NAME_MISSING` ·
  `SKILL_DESCRIPTION_MISSING` · `DOC_PATH_NOT_FOUND` · skills/ 갈래 `COMMAND_PREFIX_MISMATCH`.
  임시 확인은 사라지므로 **negative 픽스처 24~29로 영구화**했다(케이스 2·3·11만 있던 상태에서 +6).
- **엔드투엔드로 파이프라인을 돌렸다.** draft 쓰기(exit 0) → fact-checked 쓰기(exit 0) →
  `--secret-scan`(exit 0) → 렌더(exit 0). 마크다운에 배지 `근거 부족 - 미검증`과 커버리지
  3수치(7/9/12)가 실렸고, 병합이 `locked: [false, false]`를 채웠으며 `attempts: [1, 2]`가
  보존됐다. `state.json` 레지스트리에 `{path, schemaVersion, generatedBySkill}` 기재 확인.
- **게이트 A 체크박스를 정정했다(`4a31b33`).** A-1~A-5가 `[ ]`로 남아 있었으나 스키마 변경은
  `299315b`(슬라이스 B 착수 전 게이트 회차)에서 이미 반영돼 있었다. 각 항목을 코드로 확인한 뒤
  표시를 맞추고 근거를 줄마다 적었다. **「미완」으로 읽으면 이미 한 일을 두 번 한다.**
- **README를 고치다 `DOC_PATH_NOT_FOUND`에 걸렸다(`e4e5a51`).** 미구현 경로를 백틱으로 적었더니
  lint가 2건 FAIL을 냈다 — 그 검사가 처음으로 실제 대상 위에서 일한 순간이다. 이 README는
  실재 경로는 백틱, 계획된 경로는 이탤릭으로 구분한다.
- **변이 총 22종.** B-7 6종 + 콜드 리뷰 8종(S1~S8) + 프롬프트 8종(P1~P8). 각 변이가 겨냥한
  단언만(또는 실제 폭발 반경만) 깨지는 것을 실측했다.

## In Progress

**`.harness/state.json`이 없다 — 결함이 아니라 사용자 결정이다.** 이 프로젝트는 `/harness` 상태
기계 밖에서 진행한다. 따라서 `Skill`/`Task`/`Phase`/`Mode`/`Docs` 고정 라벨 기록이 이 문서에 없고,
`/handoff resume`의 Step 3.5는 축소 검사로 "legacy handoff — task state not machine-verifiable"을
보고한다. 그것이 정상이다.

이 세션에서 진행 중이던 것은 없다. 네 회차를 각각 커밋하고 4게이트 녹색·워킹 트리 clean 상태에서
정지했다.

## Blockers / Risks

- **콜드 리뷰 미반영 11건이 다음 회차의 입력이다.** 전량 리포트는
  `docs/harness/f029375/review_report.md`(라운드 1 기록 — 수정하지 마라). 이번에 반영된 것은
  Security #11 · Correctness #7 · Correctness #9 · Testing #3 넷이며, Testing #8의 「깨진 JSON
  경로 미검증」은 WA-21의 전제라 함께 닫혔다. 남은 것:
  - **(Maintainability #4) `project-ledger.mjs`의 `--out` 쓰기만 오류 처리 관례를 벗어난다** —
    `try/catch` 없이 계약 밖 종료 코드로 죽는다. **이번에 고친 바로 그 줄들 옆에 있지만
    사용자가 고른 범위 밖이라 손대지 않았다.** 없는 디렉터리를 `--out`으로 주면 여전히 원시
    스택으로 죽는다.
  - **(Architecture) prev 유래 노드의 스키마 위반이 exit 1로 나가 잘못된 복구 절차를 지시한다** —
    「출력을 고쳐 다시 부르라」는데 그 위반은 이전 산출물의 locked 노드에서 왔다. **프롬프트가
    `--force`를 지시하기 시작했으므로 이 경로의 도달 가능성이 올라갔다.**
  - **(Architecture) `state.json` 레지스트리의 read-modify-write에 동시성 제어가 없다.**
  - **(Maintainability) 드리프트 가드가 없는 사본 3건** — `EMPTY_REGISTRY_ARTIFACTS`,
    `STATE_SCHEMA_VERSION`, `EVIDENCE_FILE_NAME`(정본이 생산자가 아니라 소비자 CLI에 있다).
  - **(Maintainability) evidence contentHash가 `content-hash.mjs`와 이중 구현**이다 — 공통 하위
    헬퍼 `canonicalHash(fields, instance)`로 절차만 단일화하고 필드 목록은 각자 선언하라는 제안.
  - **(Testing) `PREV_ARTIFACT_HASH_MISSING` 보류 사유가 여전히 미검증이다**(`UNREADABLE`은 이번에
    WA-21·WA-22로 닫혔다).
  - suggestion 잔여: `computeArtifactContentHash`의 `instance` 부재 fail-closed, 보류 사유의
    기계 판독 구조화(`--json`), CLI 상투구 공통화.
- **SP 계열은 전부 소스 스캔이라 보조 방어다 — 집행이 아니다.** 명령 문자열이 문서에 있는지만
  보므로, 프롬프트가 그 줄을 적어 두고 **실행하지 말라고 덧붙이거나 실행 후 결과를 무시하면**
  통과한다. 「실제로 호출했는가」는 계측 없이 관측할 수 없다(LP-1에서 실측한 한계와 같다).
  SKILL.md는 그 점을 「'검증은 돌렸다'고 말하면서 결과를 무시하는 것이 가장 나쁜 실패」로 본문에
  적어 뒀지만, 그것은 문장이지 게이트가 아니다.
- **경로 경계가 막지 못하는 것.** `.devcareer` 세그먼트만 보므로 경계 **안에서의 오배치**는
  통과하고, 사용자가 `.devcareer`라는 이름의 디렉터리를 아무 데나 만들어도 통과한다.
- **C-5 판정이 산출물 단위다** — 노드 99개가 `insufficient`이고 1개만 external이면 집행 1건으로
  PASS가 된다. 노드 단위 커버리지는 AC-13 배지 몫으로 넘겼고 구현 8단계에서 다시 봐야 한다.
- **콜드 리뷰 T4 14건은 슬라이스 C로 이연 확정**(`a228329`, `slice_plan.md`에 기록). **예외를
  앞당겨 받는 유일한 조건은 그 항목이 그 회차의 작업을 실제로 막을 때**다.
- **도그푸딩 대상 레포가 미확정이다 — 사용자 결정이 필요하다.** 200커밋 이상·다중 저자·한글 커밋
  메시지가 있는 실제 레포. **이 단계가 타인 PII가 픽스처가 아니라 실물로 처음 흐르는 지점이다.**
- **(f)축의 잔여 위험은 그대로다** — allow-list 대조는 URL이 **목록 소속인지만** 확인한다.
  실재성과 서술 뒷받침은 검사하지 않으며(AC-1 「의존성 0」·오프라인 전제), 막는 것은 2단
  팩트체크뿐이다.
- **스펙에만 적히고 코드는 없는 항목** — C-1(오염 스위트 실행 모델), m-1(`state.artifacts.evidence`를
  쓰는 주체), m-2.
- **미검사로 남은 영역** — `plan_critic_findings.md`의 「미검사 영역」 표. **구현 7단계는 콜드
  리뷰 1라운드를 받았지만 프롬프트 계층은 아직 0회다** — 8~12단계도 여전히 0회다.
- **원격 미푸시** — 로컬 30커밋. 사용자가 명시적으로 보류를 선택했다(public 레포, 슬라이스 B·C 미완).

## Next Steps

1. **프롬프트 계층에 콜드 리뷰를 돌려라.** `skills/career-from-git/` 3종과 이번 회차의 스크립트
   변경(`store.mjs` 경계 · `write-artifact.mjs` existence 3상태 · `project-ledger.mjs` 경계)은
   **아직 한 번도 냉정한 외부 검토를 받지 않았다.** 구현 7단계 ①②가 20건을 받은 것과 대비된다.
   범위를 이번 4커밋(`0b44f24..e4e5a51`)으로 좁혀라 — 슬라이스 A 파일 재검토는 제외한다(T4는
   슬라이스 C 몫). **`review_report.md`를 수정하지 마라**; `f029375` 대상으로 다시 돌리면 라운드
   2가 자동 감지되지만 이번 범위는 다른 대상이므로 새 리포트가 난다.
2. **구현 7단계 잔여 — 범위 확정 대화의 실제 구현.** SKILL.md 0단계가 절차를 적었지만
   `git shortlog -sne` 파싱 → 저자 다중 선택 UI → `config.json` 저장의 **실행 경로가 아직 없다.**
   `schemas/config.schema.json`의 8개 required 필드를 실제로 채우는 코드가 어디에도 없으므로,
   설정 파일을 쓰는 주체를 먼저 정하라(스크립트인가 스킬인가 — 쓰기 경계 논리와 같은 질문이다).
   편집 병합의 사용자 대면 절차(exit 3 → 사용자에게 보여 줄 문구)도 여기 붙는다.
3. **구현 8단계 — `skills/skill-gap/`.** **여기서 (f)축이 처음으로 대상을 갖는다.** 구현 8단계
   (d)의 「L2·L3에서 `verification`이 무엇을 반증하는가」 정의를 프롬프트에 실제로 반영해야
   잔여 위험이 줄어든다. 렌더러·쓰기 경계 모두 계층 중립이므로 `LAYER_TITLES`와 진입점만
   늘리면 된다. **SP 계열 오라클은 `skills/**` 전체를 스캔하므로 새 스킬에 자동으로 적용된다** —
   새 템플릿이 티어·출력 언어를 안 적으면 SP-7·SP-8이 바로 FAIL한다.
4. **구현 9단계 착수 전에 C-1(오염 스위트 실행 모델)을 스펙에 명문화하라.** 스킬 실행 3회는
   사람이 수행해 산출물을 `tests/contamination/runs/<run-id>/`에 남기고 `--contamination`은
   채점만 한다.
5. **구현 10단계 착수 전에 도그푸딩 대상 레포를 확정해 `spec.md`에 기재하라.**

## Definition of Done

**에픽 전체**: `slice_plan.md`의 3슬라이스가 모두 완료되고, 사용자 본인 Git 히스토리로 생성한 경력
기술서가 "이 정도면 실제로 쓸 수 있다" 수준이며, 갭 분석이 "공감되고 우선순위가 명확하다"고 느껴지고,
생성된 코테 문제가 실제 경력과 논리적으로 연결되며, 할루시네이션으로 인한 잘못된 지식 설명이 거의
없는 상태.

**슬라이스 B 단독**: `/devcareer-prep:career-from-git`과 `/devcareer-prep:skill-gap`이 동작하고,
오염 주입 스위트 40건이 `AC-8` 기준을 만족하며, 4게이트가 녹색인 상태.

**이 세션이 유지·추가한 완료 조건**:
- 새 제약을 넣을 때마다 **그 절이 실제로 FAIL을 내는 것을 절 단위로 관측한다.**
- **완화(제약을 넓히는 변경)는 허용 방향도 관측한다.**
- **구조적 변경(추출·단일화)은 소스 스캔으로 관측한다.**
- **계약 검사는 그 픽스처가 스키마를 실제로 통과하는지 함께 단언한다.**
- **리터럴 드리프트 가드의 닻은 그 리터럴을 정의한 모듈 밖에 둔다.**
- **변이가 섹션을 예외로 중단시키면 관측 장치가 고장난 것이다.** 파일·필드 부재를 예외가 아니라
  각 단언의 FAIL로 떨어뜨려라.
- **소스 스캔 단언은 「이름이 등장하는가」가 아니라 「호출 지점이 있는가」를 물어라.**
- **양방향 단언은 각자 다른 것을 보게 써라.** 둘 다 개수를 세면 방향을 구별할 수 없다.
- **새 제약을 넣을 때 기존 스키마 제약과 곱해 보라**(M-1이 그 곱을 보지 못해 생겼다).
- **(신규) 금지 방향 단언 앞에 「대상이 존재하는가」를 먼저 단언하라.** 대상이 0건이면 금지는
  공허하게 통과한다 — 변이 P8이 SP-3 하나만 녹색으로 남기는 것으로 실측했다.
- **(신규) 상태를 늘렸으면 그 상태 값 자체를 단언하라.** 동작이 같은 두 상태는 동작만 보는
  단언으로 구별되지 않는다 — 변이 S6이 WA-22·WA-23을 둘 다 통과한 것으로 실측했다.
- **(신규) 픽스처 헬퍼가 오버라이드를 조용히 버리게 두지 마라.** 담을 수 없는 값을 받으면
  **던져라** — B-7 회차에 WA-16이 아무것도 심지 못한 채 녹색이던 형태다.
- **(신규) 우회 수단을 설계하기 전에 테스트가 진짜 불변식을 만족하게 만들 수 있는지 보라.**
  환경변수·플래그 우회는 오케스트레이션이 조립할 수 있으므로 자기면제 통로가 된다.

## Reading Order

1. `docs/handoff/2026-08-20-slice-b-prompt-layer-gates-closed.md` — 이 문서. 현재 위치와 남은 것.
2. `skills/career-from-git/SKILL.md` — 이번 회차의 본체. 9단계 절차와 종료 코드 분기표.
   「이 스킬이 지켜야 하는 것」 4개 항이 집행 코드와 1:1로 대응한다.
3. `docs/devcareer-prep-plugin/slice_b_spec_review.md` — 뒤쪽 **'착수 전 게이트 체크리스트'**.
   **게이트 A(정정됨)·B·C·E가 전부 `[x]`**이고, **D(도그푸딩)만 열려 있다.** B-7·E-3·E-4의
   닫힘 사유와 변이 관측 결과가 각 항목에 적혀 있다.
4. `docs/harness/f029375/review_report.md` — 콜드 리뷰 라운드 1 전량 20건. **미반영 11건이 남았다.**
   「오케스트레이터 실측 기록」 절에 재현 절차가 있다. **수정하지 마라.**
5. `tests/run-smoke.mjs`의 `runSkillPromptContractSmoke` — SP-1~SP-8. 절 주석이 **왜 이 축이
   보조 방어인지**와 남는 구멍을 적고 있다. 5900줄이 넘으므로 함수 단위로 잘라 읽어라.
6. `scripts/lib/artifact-contract.mjs` — 계약 정본. `checkAuthorshipContract`의 `locked` 절과
   `mergeArtifact`의 규칙 1~5 주석에 **M-1과 B-7이 왜 같은 형태인지**가 적혀 있다.
7. `scripts/write-artifact.mjs` — 쓰기 경계. 파일 헤더의 5분기 종료 코드 계약과
   `inspectPreviousArtifact`의 **`existence` 3상태 주석**(왜 boolean이 거짓말이었는지).
8. `scripts/lib/store.mjs`의 `checkStorageBoundary` — 경로 경계. **왜 `resolveStorageRoot`
   일치가 아니라 세그먼트인지**와 **왜 테스트용 우회를 만들지 않았는지**가 함수 주석에 있다.
9. `docs/devcareer-prep-plugin/spec.md` — 실행 스펙 정본(22 AC / 12 구현단계). 131KB이므로
   통독하지 말고 `awk '/^\*\*8\. /,/^\*\*9\. /'` 같은 방식으로 필요한 단계만 잘라 읽어라.
10. `docs/devcareer-prep-plugin/slice_plan.md` — 3슬라이스 분할, 슬라이스 A 파일 수정
    예외 5건(전부 소비됨), **T4 14건의 슬라이스 C 이연 결정**.
11. `docs/devcareer-prep-plugin/conventions.md` — 규약 문서(70줄). 이 레포엔 `CLAUDE.md`가
    없다 — 필요하면 레포 루트 `CLAUDE.md`로 승격을 검토하라.

## Do NOT

- **`/harness`를 쓰지 마라 — 이 프로젝트는 상태 기계 밖에서 진행한다(사용자 결정).**
- **`slice_plan.md`의 예외 5건 밖에서 슬라이스 A 파일을 수정하지 마라. 5건은 모두 소비됐다.**
  T4 나머지는 슬라이스 C로 이연됐다 — **그 항목이 그 회차 작업을 실제로 막을 때만** 예외를 받아라.
- **`review_report.md`를 수정하지 마라** — 라운드 1 기록이다.
- **`skills/**` 안에 원장 원본 파일명 리터럴을 쓰지 마라.** SP-3이 막는다. 산문에서도 「원장 원본」
  이라고 부르고, 경로가 필요하면 `--root` 모드를 쓰거나 수집기 출력에서 받아라.
- **생성 출력이 `verification`이나 `locked`를 담게 하지 마라.** 값이 아니라 **필드의 존재**가
  금지다. 둘 다 병합이 채운다.
- **`locked` 금지를 단계나 계층으로 완화하지 마라.** fact-checked를 조립하는 주체도 같은
  오케스트레이션이고, 병합 규칙 1은 `plan`을 포함한 모든 계층에 있다.
- **잠긴 prev 노드를 writer로 심으려 하지 마라.** 픽스처 헬퍼가 던진다 — 파일을 직접 편집하라.
- **경로 경계에 테스트용 우회(환경변수·`--allow-root`)를 추가하지 마라.** 그 값은
  오케스트레이션이 조립할 수 있어 자기면제 통로가 된다. 테스트 루트가 불변식을 만족하게 하라.
- **`nodes` 비배열을 빈 배열로 강등하지 마라.** 기입 주체 검사와 병합 **양쪽**이 거부해야 한다.
- **읽기 실패를 '파일 있음'으로 단정하지 마라.** `existence`는 3상태이고, 그 값 자체를 단언하는
  WA-24를 지우면 3상태가 이름만 셋인 2상태가 된다(변이 S6 실측).
- **`--force` 백업 실패를 미처리 예외로 두지 마라.** exit 1로 위장돼 호출자를 무의미한 재시도
  루프로 유도한다(실측: exit 1 + 원시 `node:fs` 스택).
- **exit 3을 `--force`로 자동 재시도하지 마라.** 사용자 편집을 덮어쓰는 결정이며 기계가 대신할 수 없다.
- **새 검사를 넣고 "영역당 한 번" 변이로 관측했다고 보고하지 마라.** 절 단위로 하나씩 지워보고
  **대응하는 단언만** FAIL하는지 확인하라.
- **금지 방향 단언만 두지 마라 — 대상 존재와 허용 방향을 함께 두어라.**
- **변이가 섹션을 중단시키는데 그대로 두지 마라.** 부재를 예외가 아니라 FAIL로 떨어뜨려라.
- **소스 스캔을 이름 존재로 쓰지 마라 — 호출 지점을 요구하라.**
- **양방향 단언이 둘 다 같은 것(개수)을 보게 쓰지 마라.**
- **계약 검사를 만들 때 그 픽스처가 스키마를 통과하는지 함께 단언하라.**
- **리터럴 드리프트 가드의 닻을 그 리터럴을 정의한 모듈 안에 두지 마라.**
- **단언을 전체 위반 수(`length === 0`)로 쓰지 마라.** 겨냥한 경로의 위반만 보라.
- **기존 단언이 새 변경을 반증하면 그 단언을 고쳐 맞추지 마라 — 변경을 좁혀라.** 예외는 그
  단언이 기술하던 **설계 자체가 모순임이 밝혀진** 경우이며(M-1), 그때는 바꾼 이유를 두 지점에 적어라.
- **README에서 미구현 경로를 백틱으로 적지 마라** — `DOC_PATH_NOT_FOUND`가 FAIL을 낸다.
  실재 경로는 백틱, 계획된 경로는 이탤릭이다.
- **게이트 A를 「미완」으로 읽지 마라** — 체크박스는 정정됐고 근거가 줄마다 적혀 있다.
- **배지를 `basis`에서 파생시키지 마라.** AC-13 (ii)가 금지한다 — `verification`에서만 파생한다.
- **배지 리터럴을 한 곳만 고치지 마라** — 4곳(스키마 3 + `spec.md`)이 드리프트 가드로 묶여 있다.
- **`redact.mjs`·`schema-validate.mjs`·`lang-lint.mjs`를 수정하지 마라** — 예외 목록에 없다.
- **`--secret-scan`의 면제를 필드 단위로 넓히지 마라.** 값 전체가 단일 이메일일 때만 면제한다.
- **allow-list 대조를 문자열 prefix로 바꾸지 마라.** origin 정확 일치 + pathname prefix + https 강제.
- **조건부 제약을 최상위 `if/then`이나 `anyOf`로 쓰지 마라** — 이 레포의 관례는 `allOf` 원소다.
- **`excluded` 커밋의 원장 전량 등재를 되돌리지 마라. T3의 PII 3필드 축소도 되돌리지 마라.**
- **`samplingMethod` 정본 리터럴을 한 곳만 고치지 마라**(4곳이 드리프트 가드로 묶여 있다).
- **`fixtures/golden/sampling-300.expected.json`을 수집기 출력으로 덮어쓰지 마라.**
- **`redact.mjs` 패턴을 손댈 때 "40자 hex 커밋 SHA는 마스킹되지 않는다" 단언을 지우지 마라.**
- **"현재 픽스처에서 문제가 안 난다"를 회귀 없음의 근거로 쓰지 마라. 픽스처를 세계로 착각하지 마라.**
- **자기충족 테스트를 만들지 마라.** 범용 오류 코드를 쓰는 케이스는 위반 메시지 조각까지 단언하라.
- **에이전트 리포트를 그대로 받지 마라.** 이번 회차의 콜드 리뷰 4건도 전부 직접 실행해 재현을
  확인한 뒤에 반영했다(exit 1 위장은 두 상태를 나란히 돌려 비교했다).
- **도그푸딩 대상 레포 이름을 지어내지 마라.** 미확정이며 사용자가 정해야 한다.
- **원격에 푸시하지 마라** — 사용자가 명시적으로 보류를 선택했다.
- `LICENSE`(MIT)와 `.gitignore`를 수정하지 마라. `package.json`의 `npm test` 배선을 되돌리지 마라.
- **PowerShell here-string(`@'...'@`)을 Bash 도구에 쓰지 마라.** 긴 커밋 메시지는 파일에 쓰고
  `git commit -F <file>`로 넘겨라(`-F`와 `-m`은 함께 못 쓴다 — 서명 줄도 파일에 이어 붙여라).
  **Python 스크립트를 콘솔로 돌릴 때는 `sys.stdout.reconfigure(encoding="utf-8")`를 넣어라.**
- **변이 하네스를 도구 타임아웃보다 길게 돌리지 마라.** 스모크 1회가 ~2분이라 5회 이상을 한
  호출에 묶으면 `finally`가 돌기 전에 죽고 **변이가 워킹 트리에 남는다**(이번에 두 번 났다).
  2~3개씩 끊고, 원본 사본을 스크래치패드에 먼저 떠 둬라.
- **Bash 도구의 heredoc에 백슬래시를 그대로 쓰지 마라** — `\\\\`가 접혀 패턴이 안 맞는다.
  정규식 리터럴을 다룰 때는 `chr(92)*2`처럼 조립하라(이번에 실측).

## Progress Ledger

| Epic | Slice | Status | Evidence | Notes |
|------|-------|--------|----------|-------|
| devcareer-prep-plugin | slice-a-deterministic-foundation | done | 97675e7b1a9204aa722584d9917d9cde07db89de | 구현 1~6단계 + 콜드 리뷰 T1·T2·P8 반영. 4게이트 녹색(lint 0 / 201 / 19 / 11). T3·T4 미반영. 원격 미푸시. |
| devcareer-prep-plugin | slice-b-gate-a-t3-prework | done | 299315b87a9cb827cb7861210debc0a3b4cc5750 | 슬라이스가 아니라 슬라이스 B **착수 전** 스펙·계약 개정. 심사 16건 → 게이트 A·B + T3 반영 → 적대 검증 4렌즈. 4게이트 녹색(lint 0 / 257 / 23 / 11). **위 slice-a 행의 Notes는 그 시점 기록이라 T3 미반영으로 남아 있으나 이 행 이후 T3는 반영됐다** — 근거 커밋만 보고 현재 트리를 판단하지 마라. |
| devcareer-prep-plugin | slice-b-p0-skill-layer | in-progress | e4e5a51345763143a36b666751ba42ab57c8beef | **구현 7단계 완료(결정적 진입점 + 프롬프트 계층) + 열린 게이트 3건 전부 닫힘.** 이전 회차들이 예외 1~5·렌더 계약·진입점 3종·콜드 리뷰 3건을 닫았고, **이 세션이 게이트 B-7(`locked` 기입 주체) · E-3 나머지 절반(양방향 소스 스캔) · E-4 신설(`--stage` 자기 선언)을 닫고 `skills/career-from-git/` 3종을 세웠다.** 콜드 리뷰 4건(Security 1 · Correctness 2 · Testing 1) 추가 반영. 휴면 검사 6종에 negative 픽스처 24~29 신설. 4게이트 녹색(lint 0 / **433** / **33** / 11). 변이 22종으로 절 단위 관측. 엔드투엔드(쓰기→렌더) 실행 확인. **미착수: 범위 확정 대화의 실행 경로 · 구현 8~10단계 · 프롬프트 계층 콜드 리뷰(0회).** **콜드 리뷰 미반영 11건**이 다음 회차 입력. 게이트 D(도그푸딩)만 열려 있음. T4 14건은 슬라이스 C 이연. 원격 미푸시(30커밋). |

## Resume
Run: `/handoff resume docs/handoff/2026-08-20-slice-b-prompt-layer-gates-closed.md`
