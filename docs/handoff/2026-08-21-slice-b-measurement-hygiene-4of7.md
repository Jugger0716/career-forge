# HANDOFF — 슬라이스 B: 측정기 위생 아크 6/7 완료 + 문서 부패 정정 + 순서 정본화

**Date:** 2026-08-21  **Project:** career-forge (devcareer-prep 플러그인)
**Branch:** harness/devcareer-prep-plugin-2  **HEAD:** 0c3d76642a96c03889377c14058a7d0c0ab22d2c — fix(smoke): stop the ignored-path guard from reading an unread file as clean
**Dirty:** clean  **Upstream:** `origin/harness/devcareer-prep-plugin-2` 대비 **6커밋 앞섬(미푸시)**. `origin/main` 대비 42커밋 앞섬, 병합·PR 없음.

## Goal

개발자의 Git 히스토리를 분석해 경력 기술서 → 지식맵 → 갭 리포트 → 학습·코테 계획을 만드는
Claude Code 플러그인(`devcareer-prep`)을 구축한다. 핵심 가치는 **할루시네이션 방지** — 모든 사실적
주장이 실존하는 커밋 해시나 신뢰 가능한 출처로 뒷받침되어야 하고, 그 검증을 LLM 판단이 아니라
**결정적 스크립트**가 수행한다.

에픽은 `foundation-first` 3슬라이스다. ①은 완료됐고 ②가 진행 중이다. **이 세션은 코드 기능을
늘리지 않았다** — 관측 장치(스모크)의 결함 3건을 닫고, 문서가 코드보다 더/덜 주장하던 곳 7건을
정정하고, 남은 작업 순서를 추적 문서에 정본화했다.

## Current State (verified)

- **커밋 6개** — `31ac2f7`(측정기: 섹션 예외 중단) → `1415020`(문서 정정 7건 + 결정 D1~D4)
  → `5c1a818`(순서 정본화) → `ea40bc6`(측정기: 모듈 최상위 판독) → `0a42457`(총량·중단 가드 신설)
  → `0c3d766`(측정기: 조용한 강등). `git log --oneline 8f82d28..HEAD`로 확인. 워킹 트리 clean.
- **4게이트 녹색이고 수치가 바뀌었다** — `npm run lint` exit 0 / 스모크 **448 PASS** /
  `--negative` **35 PASS** / `--golden` **13 PASS**, 전부 0 FAIL. 세션 시작은 445/33/11이었다.
  **증가분은 신규 단언 1건(DH-1d) + 모드별 스위트 가드 2건**이다.
- **커밋 6개 전부를 새 클론에서 검증했다.** `git clone --no-hardlinks` 후 그 안에서 4게이트를
  돌렸다(마지막 확인: lint 0 / 448 / 35 / 13). `docs/harness/`가 클론에 없는 것도 확인했다.
- **A류(모듈 최상위 맨몸 판독) 0건.** 세션 시작 시 1건(`EVIDENCE_SCHEMA`)이었고, 그 1건은
  파일 부재 시 **import 시점에 프로세스를 죽여 단언 0건**으로 끝나게 만들고 있었다(실측).
- **B류(섹션 내부 맨몸 판독) 11건은 그대로다.** 세션 중 재집계로 확인했다 —
  512·640·1055·1136·1955·2069·2334·2531·3057·3187·3818행. 이번에 닫은 것은 별개의
  「조용한 강등」 1건이다.
- **스위트 가드 2건이 신설됐다** — 모드별로 (i) 예외 중단 섹션 0건 (ii) 단언 총량 정확 일치.
  정본 상수는 `EXPECTED_ASSERTIONS_BEFORE_GUARDS`(가드 제외 수: 446/33/11)다.
- **그 가드가 같은 세션에서 실제로 값을 증명했다.** 6번에서 단언을 1건 늘리자
  `단언 446건(기대 445건, +1) — 의도한 추가라면 EXPECTED_ASSERTIONS_BEFORE_GUARDS를 함께 고쳐라`
  로 FAIL하며 고칠 상수까지 지목했다. 그 이전에는 총량 변화가 아무 신호도 내지 않았다.
- **거짓 초록 1건을 실측으로 증명하고 닫았다.** 추적 파일 하나를 워킹 트리에서만 지운 사본에서
  **수정 전 447 PASS / 0 FAIL / exit 0**(DH-1b가 「참조 없음」으로 조용히 PASS),
  **수정 후 DH-1d만 FAIL / exit 1**. 하필 「워킹 트리는 녹색인데 새 클론에서만 깨진다」를 막으려고
  만든 가드가 같은 방식으로 스스로 무력화돼 있었다.
- **문서 정정 7건은 전부 코드로 확인한 뒤 기록했다** — f029375 Major #1·#3 이미 닫힘, 게이트
  C-3·C-4·D-2 이미 충족(체크박스만 낡음), C-2 후속 표의 `externalUrl` 「없음」 3칸이 무효
  (네 계층 모두 실재), `origin`/`verification` 집행 코드 실재, `config.schema.json` required 9개.
- **실제 드리프트 1건을 새로 찾았다** — `slice_plan.md` 예외 4번 ②가 「네 파일 모두」 완화한다고
  적었는데 `schemas/plan.schema.json`만 아직 `const: "insufficient"`다. 기록만 했고 8번에서 닫는다.
- **변이 관측을 절 단위로 했다** — 이 세션 총 11종(1번 3종 · 4번 3종 · 5번 4종 · 6번 2종
  대조쌍). 전부 격리 사본에서 돌렸고 워킹 트리에 변이가 남지 않은 것을 확인했다.
- **핸드오프 원문은 고치지 않았다.** 낡은 서술의 정정은 `slice_b_spec_review.md`의
  「정정 기록 (2026-08-21)」 절에 남겼다 — 시점 기록 불변 규약.

## In Progress

**`.harness/state.json`이 없다 — 결함이 아니라 사용자 결정이다.** 이 프로젝트는 `/harness` 상태
기계 밖에서 진행한다. 따라서 `Skill`/`Task`/`Phase`/`Mode`/`Docs` 고정 라벨 기록이 이 문서에 없고,
`/handoff resume`의 Step 3.5는 축소 검사로 "legacy handoff — task state not machine-verifiable"을
보고한다. 그것이 정상이다.

진행 중이던 것은 없다. 6개 회차를 각각 커밋하고, 각 커밋을 새 클론에서 검증한 뒤 정지했다.
**측정기 위생 아크(4~7번)의 7번만 미착수다.**

## Blockers / Risks

- **콜드 리뷰 타이밍을 이 세션에서 바꿨다 — 다음 세션이 그대로 따라야 한다.** 상세는 Next Steps 2.
  요지: 4~6번이 같은 문제에 **서로 다른 세 모양**을 만들었고 7번은 그중 하나를 **11곳에 복제**하는
  일이므로, 리뷰를 7번 뒤로 미루면 틀릴 수 있는 모양의 사본 11개를 리뷰하게 된다.
- **표적 검증 6~7건이 미해결이다 — 콜드 리뷰보다 먼저 해야 한다.** `1415020`이 「이미 닫혔다」를
  단언한 것 중 **에이전트 검증 1회에만 기댄 것**이다. 목록은 `slice_b_spec_review.md`의
  「7번 직후에 몰아서 할 표적 검증」 절에 있다. 게이트 E-4가 「거짓 전제 위에서 닫혀 있던」 것과
  같은 위험이며, 검증 없이 콜드 리뷰를 돌리면 리뷰가 그 전제를 물려받는다.
- **스위트 가드의 한계 2건을 실측했고 닫지 않았다.**
  - **두 가드는 서로를 감시하지 않는다.** 중단 가드 한 줄을 지우면 총량 가드는 그대로 PASS한다
    (실측 446 PASS / **0 FAIL**) — 총량을 가드 실행 **전**에 잡으므로 가드 개수가 기준에 안 들어간다.
    실재하는 안전망은 한 방향뿐이다(카운터가 무력화되면 총량 가드가 받친다). **「가드를 세는
    가드」는 의도적으로 두지 않았다** — 메타 가드는 끝없이 재귀한다.
  - **정확 일치를 하한(`>=`)으로 완화해도 무변이 트리에서 아무것도 깨지지 않는다**(실측 447/0).
    즉 그 완화를 되돌릴 근거가 게이트에 남지 않는다.
- **`main()` 밖으로 새는 예외는 여전히 가드 밖이다.** `main().catch`가 `[중단]`을 찍고 exit 1로
  죽으며 스위트 가드는 아예 실행되지 않는다. 프로세스 사망은 종료 코드와 「결과:」 줄의 부재로만
  관측된다. 4번이 그 부류를 줄였고 더 좁힐 수단은 없다.
- **f029375 미반영 잔여의 정확한 구성이 확정되지 않았다.** 이 세션이 Major #1·#3을 닫힌 것으로
  확인했으므로 핸드오프의 「10건」과 구성이 다르다. 전량 리포트는
  `docs/harness/f029375/review_report.md`이고 **추적되지 않는다** — 새 클론에는 없다.
  `slice_b_spec_review.md`의 정정 절이 클론에 남는 유일한 백로그다.
- **`config.json` 쓰기 주체가 아직 없다.** 결정 D3으로 방향(A: config.json이 정본)은 확정됐으나
  코드는 없다. `writeConfig`의 프로덕션 호출자 0건이라 SKILL.md 0단계의 「저장한다」는 실행 경로가
  없다. 11번에서 닫는다.
- **도그푸딩 대상 레포가 미확정이다 — 사용자가 직접 지정한다(결정 D1).** 이름을 지어내면 안 된다.
  **결정 D2에 따라 실물 PII가 흐르기 전에 원격을 private으로 전환하는 것이 선행 조건이다.**
- **브랜치가 public 원격에 있고 6커밋이 미푸시다.** 푸시하면 즉시 공개된다 — PII·절대경로 스캔
  없이 푸시하지 마라. 이 세션의 6개 diff는 매번 스캔했고 전부 해당 없음이었다.
- **SP-10은 인용 검증 호출 하나만 실행한다.** 나머지 단계(1·2·4·6·8·9)의 인자 완비성은 여전히
  미관측이다. 그 조사를 이번에 한 에이전트가 도중에 죽어 결과가 없다.
- **7단계 템플릿에 노드 id 재사용 규칙·`verification` 기입 주체 규약이 실제 프롬프트 문장으로
  들어갔는지 아무도 확인하지 않았다.** 비어 있으면 구현 8단계가 없는 선례를 복제한다. 13번의
  사전 점검 항목으로 적어 뒀다.
- **스펙에만 적히고 코드는 없는 항목** — m-1, m-2. (C-1은 이미 명문화됐다 — 정정 3.)

## Next Steps

1. **표적 검증 6~7건.** `slice_b_spec_review.md`의 「7번 직후에 몰아서 할 표적 검증」 절 목록을
   각각 **코드로 반증 시도**하라. 틀린 것이 나오면 `1415020`의 해당 서술을 정정 커밋한다.
   **콜드 리뷰보다 먼저** — 리뷰가 미검증 전제를 물려받지 않게 한다. **실행 가능한 슬래시 명령은
   없다**; 반증 시도는 파일을 열고 grep하는 직접 작업이다. 규모 M.
2. **콜드 리뷰 — 7번보다 먼저 돌린다(이 세션에서 타이밍을 바꿨다).**
   `/deep-review 8f82d28..0c3d766 --mode thorough`
   **근거**: 4~6번이 같은 문제에 세 모양(`readRepoText`+`blameFor` / `{schema,error}`+삼항 /
   Map 일괄 판독+전제 단언)을 만들었고, 7번은 그중 하나를 **11곳에 복제**한다. 팬아웃 뒤에
   리뷰하면 틀릴 수 있는 모양의 사본 11개를 리뷰하게 된다. 원래 반대 근거였던 「같은 대상 재실행이
   라운드 2를 만든다」는 이점은 **슬러그가 범위 양끝이라 HEAD가 움직이면 어차피 라운드 1이 새로
   생기므로** 성립하지 않는다. **대가**: 리뷰어가 「중단 0건」이 아직 안 닫힌 상태를 본다 — 의도된
   상태이므로 지적이 나오면 그렇게 답하라. 리포트는 `docs/harness/<slug>/`에 나오고 **추적되지
   않는다** — 미반영 항목 요약을 `slice_b_spec_review.md`로 옮겨야 클론에 남는다.
3. **7번 — B류 11곳을 공유 헬퍼 1개로.** 콜드 리뷰가 고른 모양을 표준으로 삼아라. 관측 단위는
   사이트 11회가 아니라 **부재시킬 대상 7개**(evidence·career·state 스키마,
   `tests/fixtures-valid/career.json`, `collect-git-facts.mjs`, `validate-plugin.mjs`, `.gitignore`)다 —
   `career.schema.json` 삭제 1회가 4개 사이트를 동시에 관측한다. **「현실적인 대상만 고르자」는
   타협을 기각한다**; 예산이 빠듯하면 사이트를 골라 남기지 말고 **파일 단위로 커밋을 쪼개라**.
   규모 L.
4. **푸시 판단.** 6+커밋을 원격에 올릴지. 스캔은 매 커밋에서 통과했으나 **푸시 전 전수 재스캔**하라.
   `main` 병합·PR은 지시가 없었다.
5. **8번 이후는 `slice_b_spec_review.md`의 「작업 순서 (2026-08-21 확정)」 절이 정본이다.**
   핸드오프의 이 목록보다 그 절을 믿어라 — 8~15번의 근거와 완료 조건이 전부 거기 있다.

## Definition of Done

**에픽 전체**: `slice_plan.md`의 3슬라이스가 모두 완료되고, 사용자 본인 Git 히스토리로 생성한 경력
기술서가 "이 정도면 실제로 쓸 수 있다" 수준이며, 갭 분석이 "공감되고 우선순위가 명확하다"고 느껴지고,
생성된 코테 문제가 실제 경력과 논리적으로 연결되며, 할루시네이션으로 인한 잘못된 지식 설명이 거의
없는 상태.

**슬라이스 B 단독**: `/devcareer-prep:career-from-git`과 `/devcareer-prep:skill-gap`이 동작하고,
오염 주입 스위트 40건이 `AC-8` 기준을 만족하며, 4게이트가 녹색인 상태.

**측정기 위생 아크(4~7번) 단독**: 스키마·픽스처·`.gitignore` 중 어느 파일을 지워도 (i) 프로세스가
죽지 않고 (ii) 섹션이 예외로 중단되지 않고 (iii) 그 파일에 의존하는 단언만 사유와 함께 FAIL하며
(iv) 단언 총량이 유지되는 상태. **현재 (i)은 달성, (ii)는 미달(중단 2건 — 7번이 닫는다),
(iii)(iv)는 닫은 지점에 대해 달성.**

**이 세션이 유지·추가한 완료 조건** (기존 목록은 직전 핸드오프 참조):
- **(신규) 완료 조건을 스스로 어기지 않았는지 확인하라.** 4번의 완료 조건 (ii)를 「중단 0건」으로
  적었는데 4번 단독으로는 달성 불가능한 조건이었다 — A류(프로세스 사망)와 B류(섹션 중단)의 결과를
  한 줄에 뭉뚱그린 것이다. **조건을 못 지켰으면 성공을 선언하지 말고 조건을 정정하라.**
- **(신규) 총량이 바뀌면 정본 상수를 함께 고쳐라.** 이제 기계가 강제한다 — 안 고치면 총량 가드가
  차이와 고칠 곳을 함께 찍고 FAIL한다.
- **(신규) 「부재를 예외로 두지 마라」의 반대 얼굴도 막아라.** 판독 실패를 빈 문자열·빈 배열로
  강등하면 금지 방향 스캔이 **위반 0건으로 집계해 조용히 PASS**한다. 시끄러운 실패보다 나쁘다.
- **(신규) 전제 단언을 하나에 묶지 마라.** 「대상이 존재하는가」(DH-1a)와 「그 대상을 실제로
  읽었는가」(DH-1d)는 따로 둔다 — 어느 경로로 실패했는지가 로그에 고정된다.
- **(신규) 문서 주장을 코드로 확인한 뒤에만 「닫혔다」고 적어라.** 그리고 **에이전트 검증 1회에만
  기댄 것과 직접 재현한 것을 구별해 적어라** — 이 세션이 그렇게 갈라 기록했고, 갈라 적지 않았으면
  6~7건이 확정처럼 굳었다.
- **(신규) 자기 주장이 변이로 반증되면 주석·문서를 실측에 맞춰 좁혀라.** 「두 가드는 서로를
  감시한다」가 G2에서 반증됐다. 그 문장을 남겨 두면 `4cb236b`가 고친 「코드보다 더 주장하는 주석」의
  새 사례가 된다.

## Reading Order

1. `docs/handoff/2026-08-21-slice-b-measurement-hygiene-4of7.md` — 이 문서. 현재 위치와 남은 것.
2. `docs/devcareer-prep-plugin/slice_b_spec_review.md` — **뒤쪽 두 절이 이 프로젝트의 정본이다.**
   「정정 기록 (2026-08-21)」(문서가 코드와 어긋난 곳 + 결정 D1~D4 + 표적 검증 목록)과
   「작업 순서 (2026-08-21 확정)」(15항 + 각 항목 규모·완료 조건 + 갈렸던 지점). 그 앞의 게이트
   체크리스트도 함께 보라 — C-3·C-4·D-2가 이번에 `[x]`로 정정됐다.
3. `tests/run-smoke.mjs`의 `finishMode`와 `EXPECTED_ASSERTIONS_BEFORE_GUARDS` — 스위트 가드
   2건과 그 한계 2건이 주석에 실측으로 적혀 있다. 6600줄이 넘으므로 함수 단위로 잘라 읽어라.
4. `tests/run-smoke.mjs`의 `runIgnoredPathReferenceSmoke` — DH-1a~DH-1d. **거짓 초록이 어떻게
   생겼고 왜 전제 단언을 둘로 나눴는지**가 주석에 있다. 7번이 따를 후보 모양 중 하나다.
5. `tests/run-smoke.mjs`의 `loadEvidenceSchema`와 `runSamplingLiteralDriftSmoke` — 나머지 두 후보
   모양. 7번이 이 셋 중 하나를 골라 표준화한다.
6. `docs/devcareer-prep-plugin/conventions.md` — 규약. §9가 문서 위치 규약과 도구 동작 2건을 적는다.
   이 레포엔 `CLAUDE.md`가 없다(순서 12번이 그 항목이다).
7. `skills/career-from-git/SKILL.md` — 오케스트레이션 9단계. 7단계의 `--identity`와 exit 2 양의성
   경고. 「이 스킬이 지켜야 하는 것」 4개 항이 집행 코드와 1:1 대응한다.
8. `docs/devcareer-prep-plugin/spec.md` — 실행 스펙 정본(22 AC / 12 구현단계). **131KB이므로
   통독하지 말고** `awk '/^\*\*8\. /,/^\*\*9\. /'` 같은 방식으로 필요한 단계만 잘라 읽어라.
   **드리프트 가드 2건이 기계로 읽는 닻이다.**
9. `docs/devcareer-prep-plugin/slice_plan.md` — 3슬라이스 분할, 예외 5건(전부 소비됨), T4 14건 이연.
10. `docs/handoff/2026-08-20-slice-b-coldreview-closed-docs-moved.md` — 직전 핸드오프.
    **Do NOT 목록이 여전히 유효하다**(아래는 그 위에 이 세션이 더한 것만 적는다). 다만 그 문서의
    Next Step 4와 f029375 Major #3 서술은 **낡았다** — 정정은 항목 2의 정정 절에 있다.

## Do NOT

직전 핸드오프의 Do NOT 목록이 **전부 유효하다.** 아래는 이 세션이 더한 것이다.

- **`/harness`를 쓰지 마라 — 이 프로젝트는 상태 기계 밖에서 진행한다(사용자 결정).**
- **`main`에 병합하거나 PR을 열지 마라.** **PII·절대경로 스캔 없이 푸시하지 마라.**
- **핸드오프 원문을 고치지 마라** — 시점 기록이다. 낡은 서술은
  `slice_b_spec_review.md`의 정정 절에 남긴다.
- **콜드 리뷰를 7번 뒤로 미루지 마라** — 이 세션이 근거를 대고 앞으로 당겼다(Next Steps 2).
  미루면 틀릴 수 있는 모양의 사본 11개를 리뷰하게 된다.
- **표적 검증 없이 콜드 리뷰를 돌리지 마라** — 리뷰가 미검증 전제를 물려받는다.
- **`EXPECTED_ASSERTIONS_BEFORE_GUARDS`를 하한(`>=`)으로 완화하지 마라.** 무변이 트리에서
  아무것도 깨지지 않으므로(실측) 되돌릴 근거가 게이트에 남지 않는다.
- **「가드를 세는 가드」를 만들지 마라.** 메타 가드는 끝없이 재귀한다. 두 가드가 서로를 감시하지
  **않는다**는 사실은 주석에 실측으로 적혀 있고, 그것이 이 축의 한계다.
- **판독 실패를 빈 문자열·빈 배열·`null`로 강등하지 마라.** 금지 방향 스캔이 위반 0건으로 집계해
  조용히 PASS한다 — 이번에 실측한 거짓 초록의 정확한 형태다.
- **「추적돼 있으나 워킹 트리에 없는 파일」을 「없으니 깨끗하다」로 읽지 마라.** DH-1d가 막는다.
- **A류(모듈 최상위에서 레포 파일 판독)를 새로 만들지 마라.** 부재 시 import 시점에 프로세스가
  죽어 **단언 0건**으로 끝난다 — 섹션 중단보다 한 단계 더 나쁘다. 현재 0건이다.
- **`slice_b_spec_review.md`의 정정 절에 적힌 「이미 닫혔다」를 재작업하지 마라.** 단
  **에이전트 검증 1회에만 기댄 6~7건은 표적 검증 대상이다** — 그 절이 둘을 갈라 적고 있다.
- **`plan_critic_findings.md`의 체크박스 열을 일괄로 뒤집지 마라.** 그 파일의 `[ ]` 14개는 이
  레포 역사에서 `[x]`로 바뀐 적이 0건이고, A-4·B-5처럼 `[ ]`가 정당한 항목이 섞여 있다.
- **`spec.md`의 AC 체크박스 22건을 뒤집지 마라.** 전부 `[ ]`이고 역사 전체에서 `[x]`가 된 커밋이
  0건이다 — 「고의로 절대 체크하지 않는 닻」인지 「갱신 누락」인지 판별되지 않았다.
- **도그푸딩 대상 레포 이름을 지어내지 마라(D1).** **실물 PII 전에 원격을 private으로 전환하는
  것을 건너뛰지 마라(D2)** — §6의 T3 정책이 코드로 닫혀 있다는 사실이 그 선행 조건을 대체하지 않는다.
- **`plan.schema.json`의 `const → enum`을 새 예외로 처리하지 마라(D4)** — 예외 4번의 미완 집행이다.
- **`~/.devcareer/<repo-key>/`를 실제 사용 이력으로 오인하지 마라.** 이전 세션의 조사
  에이전트가 1·2단계 명령을 실행해 남긴 잔재다(레포 밖, 영향 없음).

## Progress Ledger

| Epic | Slice | Status | Evidence | Notes |
|------|-------|--------|----------|-------|
| devcareer-prep-plugin | slice-a-deterministic-foundation | done | 97675e7b1a9204aa722584d9917d9cde07db89de | 구현 1~6단계 + 콜드 리뷰 T1·T2·P8 반영. 4게이트 녹색(lint 0 / 201 / 19 / 11). T3·T4 미반영. 원격 미푸시. |
| devcareer-prep-plugin | slice-b-gate-a-t3-prework | done | 299315b87a9cb827cb7861210debc0a3b4cc5750 | 슬라이스가 아니라 슬라이스 B **착수 전** 스펙·계약 개정. 심사 16건 → 게이트 A·B + T3 반영 → 적대 검증 4렌즈. 4게이트 녹색(lint 0 / 257 / 23 / 11). **위 slice-a 행의 Notes는 그 시점 기록이라 T3 미반영으로 남아 있으나 이 행 이후 T3는 반영됐다** — 근거 커밋만 보고 현재 트리를 판단하지 마라. |
| devcareer-prep-plugin | slice-b-p0-skill-layer | in-progress | 0c3d76642a96c03889377c14058a7d0c0ab22d2c | **측정기 위생 아크 6/7 완료 + 문서 부패 정정 7건 + 순서 15항 정본화.** 이전 회차들이 구현 7단계와 프롬프트 계층 콜드 리뷰 1회차를 닫았고, **이 세션은 코드 기능을 늘리지 않고 관측 장치의 결함 3건을 닫았다** — (a) 섹션 예외 중단으로 단언 2건이 사라지던 것 (b) 모듈 최상위 판독으로 프로세스가 죽어 **단언 0건**이 되던 것 (c) 판독 실패를 빈 문자열로 강등해 금지 방향 스캔이 **조용히 PASS**하던 거짓 초록. 스위트 가드 2건(중단 0건·총량 정확 일치) 신설. 변이 11종 절 단위 관측. **4게이트 수치가 445/33/11 → 448/35/13으로 바뀌었다**(신규 단언 1 + 모드별 가드 2). 커밋 6개 전부 새 클론 검증. **문서 정정 7건**(f029375 Major #1·#3 이미 닫힘 / 게이트 C-3·C-4·D-2 이미 충족 / `externalUrl` 표 무효 / 집행 코드 실재 / required 9개)과 **결정 D1~D4** 기록. **새 드리프트 1건 발견**(`plan.schema.json`만 `const`). **미착수: 표적 검증 6~7건 · 콜드 리뷰(7번 앞으로 당김) · 7번 B류 11곳 · 8~15번.** A류 0건 / B류 11건. 원격 대비 **6커밋 미푸시**, `main` 미병합(42커밋 앞섬). 게이트 D는 D-1(도그푸딩 레포)만 열려 있음. |

## Resume
Run: `/handoff resume docs/handoff/2026-08-21-slice-b-measurement-hygiene-4of7.md`
