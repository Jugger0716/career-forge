---
description: 이 스킬은 Git 커밋 히스토리를 읽어 경력 기술서를 생성할 때 사용한다.
---

# name이 없는 SKILL.md

frontmatter에 `description`은 있으나 `name`이 없는 negative 픽스처(케이스 24)다.
name이 없으면 디렉터리명 대조 자체가 성립하지 않으므로, 그 부재를 조용히
넘기면 케이스 2(name↔디렉터리 불일치)가 영원히 대상 0건이 된다.
